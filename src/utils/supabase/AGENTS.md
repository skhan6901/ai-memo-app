# Supabase 클라이언트 - AI Agent 지침서

## 역할

서버 전용 Supabase JS 클라이언트 생성. App Router 서버 액션에서만 사용합니다.

## 파일

- `server.ts` — `createSupabaseServerClient()`

## 환경 변수

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
